# ================================================
# DangunDad Flutter App - pubspec.yaml Template
# ================================================
# App: mental_math_trainer
# Description: Train your brain with fast mental math.

name: mental_math_trainer
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  get: ^4.7.3

  # Local storage
  hive_ce: ^2.19.3
  hive_ce_flutter: ^2.3.4
  path_provider: ^2.1.5

  # UI
  flutter_screenutil: ^5.9.3
  flex_color_scheme: ^8.4.0
  google_fonts: ^8.0.2
  lucide_icons_flutter: ^3.1.14+2

  # Ads
  google_mobile_ads: ^8.0.0
  in_app_purchase: ^3.2.3

  # Features and monetization
  intl: ^0.20.2
  fl_chart: ^1.2.0
  confetti: ^0.8.0
  flutter_animate: ^4.5.2
  gma_mediation_applovin: ^2.6.0
  gma_mediation_pangle: ^3.6.0
  gma_mediation_unity: ^1.7.0
  in_app_review: ^2.0.12
  rate_my_app: ^2.3.2
  vibration: ^3.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_ce_generator: ^1.11.2
  build_runner: ^2.15.0
  change_app_package_name: ^1.5.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.7

flutter:
  uses-material-design: true

  assets:
    - assets/fonts/
    - assets/images/
    - assets/data/
  fonts:
    - family: NotoSans
      fonts:
        - asset: assets/fonts/NotoSans-Bold.ttf
          weight: 700

